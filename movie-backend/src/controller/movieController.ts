import { Request, Response } from 'express';

// Placeholder route for fetching movies
export const getAllMovies = async (_req: Request, res: Response) => {
  res.json({ message: 'List of all movies (to be implemented)' });
};